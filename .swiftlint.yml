excluded: # paths to ignore during linting. Takes precedence over `included`.
  - Pods
  
disabled_rules:
  - trailing_whitespace
  - statement_position
  - type_name
  - nesting
  - variable_name

force_cast: warning # implicitly

line_length: 200

file_length:
  warning: 500
  error: 1200
